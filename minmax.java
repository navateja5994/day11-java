import java.util.*;
class Main{
    public static void main(String args[])
    {
        public static void miniMaxsum(List<Integer>arr)
        {
            long mini,max,s=0;
            Collections.sort(arr);
            int n=arr.size();
            for(int i=0;i<n;i++)
            s+=arr.size(i);
            System.out.println((s-arr.get(n-1))+" "(s-arr.get(o)));
        }
    }
}